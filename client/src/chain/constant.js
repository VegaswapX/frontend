import {BSC_MAINNET, LOCAL_NET} from "./eth";

// Notice: Organize this later
const Chains = {
  BSC_MAINNET: {
    chainId: BSC_MAINNET,
  },
  LOCAL_NET: {
    chainId: LOCAL_NET,
  },
};

export {Chains};